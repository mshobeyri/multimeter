import Replaces from '../sections/Replaces'
import CTA from '../sections/CTA'

export default function Pricing() {
  return (
    <div className="pt-24">
      <Replaces />
      <CTA />
    </div>
  )
}
