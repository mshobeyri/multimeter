# Cloudflare Worker configuration for test.mmt.dev
# Deploy with: npx wrangler deploy -c wrangler-testserver.toml
name = "mmt-testserver"
main = "worker/testserver.ts"
compatibility_date = "2024-01-01"

# Custom domain (configure after first deploy)
# routes = [{ pattern = "test.mmt.dev", custom_domain = true }]
