# Board — 14 Sep 2026 — Hero YAML windows (CMO)

**Status:** CMO vote only. Not executed. Isolated from other roles.

**Role:** CMO.

## Question

Keep the two-window hero (copyable `echo.mmt` card + VS Code illustrator below), CHANGE by merging Copy into the illustrator and killing the separate card, or CUT the illustrator and keep only the echo card?

Founder suggestion: one YAML, inside VS Code chrome, with Copy. Kill the separate echo card.

## Vote

**CHANGE** — merge the try path into the illustrator. Do not KEEP two YAML windows. Do not CUT the VS Code chrome.

## Decision (CMO)

The founder is right on the duplication, wrong if “into the illustrator” means the current non-identical `api.mmt` stays. One YAML: the live echo POST to `https://test.mmt.dev/echo`. That file sits in VS Code chrome with Copy + paste-and-Send. Tabs stay click-only; test/suite remain finished reports, not a second try path.

## Dissent (anticipated)

Channel/Critic will still want CUT (HN hates mock chrome). CMO holds: chrome is the “in VS Code” half of the positioning sentence. Two windows was the error, not the chrome.

## Kill list

- Two YAML windows on first glance
- Illustrator `api.mmt` that is similar-but-not-identical to the copyable echo
- Copy on test/suite tabs (those are still a marketing mock)

## Brand risk

Copy inside a mock that also shows fake Passed reports can read as “the Send is fake too” unless the copied bytes are the real echo file and only that tab copies.

## Still not this meeting

Implementation. Show HN submit (Tue 15 Sep). Other roles’ votes.
