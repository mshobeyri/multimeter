# UI

The environment panel and edit mode are documented under [Edit](./edit.md).

That page covers:

- The environment panel (Reload, Clear, preset and variable pickers)
- Edit Environment tabs (Overview, Variables, Presets, Settings, Certificates)
- Promoting API response values with `setenv`
